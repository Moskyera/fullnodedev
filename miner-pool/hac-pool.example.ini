; Example environment-style notes for hac-pool
; Prefer CLI flags or env vars (see --help).
;
; HAC_POOL_UPSTREAM=127.0.0.1:8080
; HAC_POOL_HTTP_BIND=0.0.0.0:3333
; HAC_POOL_STRATUM_BIND=0.0.0.0:3334
; HAC_POOL_TOKEN=
; HAC_POOL_POLL_MS=2000
; Max concurrent stratum connections from one source IP (0 = unlimited)
; HAC_POOL_MAX_CONNS_PER_IP=128
